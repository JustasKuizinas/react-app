import React, { PureComponent } from 'react'

export class PureComp extends PureComponent {
    render() {
        return (
            <div className="react-object">
               Pure component
            </div>
        )
    }
}

export default PureComp
